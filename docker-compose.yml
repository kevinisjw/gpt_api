version: "3.9"

services:
  openrouter-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openrouter-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
      - ./images:/app/images
